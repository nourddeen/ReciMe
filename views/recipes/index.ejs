<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1> RECIPES </h1>
    <% if( isLoggedIn ) {%>
        <p>Hello</p>
       <% } else { %>
        <p><a href="/users/new">Click here</a> to Register an account.</p>
        <p><a href="/users/login">Click here</a> to Login.</p>
            <% } %>
    <% if(recipes.length===0) {%>
        <h2>There are no recipes yet!</h2>
        <a href="/recipes/new">Create one now!</a>
        <% } else { %>
            <ul class="recipes">
                <% recipes.forEach((recipe)=>{ %>
                    <li><a href="/recipes/<%=recipe._id%>">
                            <%= recipe.name %>
                        </a></li>
                    <% }) %>
            </ul>
            <a href="/recipes/new">Create one now!</a>
            <% } %>
                

</body>

</html>